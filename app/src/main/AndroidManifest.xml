<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jamilton.gestiondeltiempo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GestionDelTiempo">
        <activity android:name="com.jamilton.gestiondeltiempo.view.iu.AddEditEventoActivivy"></activity>
        <activity
            android:name="com.jamilton.gestiondeltiempo.MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name="com.jamilton.gestiondeltiempo.model.notificaciones.AlertReceiver" />
    </application>

</manifest>